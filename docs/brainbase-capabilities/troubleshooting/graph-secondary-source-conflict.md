# Graph Conflicts With Secondary Sources

If Graph conflicts with meeting notes, transcripts, memory, or generated summaries, prefer Graph for canonical naming and identity.

Use secondary sources to explain why the mismatch may have happened, but do not silently overwrite Graph facts from secondary evidence.
