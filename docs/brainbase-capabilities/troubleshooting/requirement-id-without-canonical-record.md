# Requirement ID Without Canonical Record

If an `FRD-*`, `REQ-*`, or `BUG-*` ID is mentioned but no canonical NocoDB or Graph record can be found, do not invent the requirement.

State the lookup gap, then proceed only from repo files, linked PRs, or user-provided text.
