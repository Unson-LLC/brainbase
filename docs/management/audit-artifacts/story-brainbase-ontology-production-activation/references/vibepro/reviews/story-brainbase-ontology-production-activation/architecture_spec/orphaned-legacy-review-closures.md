# Orphaned legacy review lifecycle closures

The following stale-HEAD review lifecycles exceeded their timeout and have no result that can be reused for the current HEAD:

- `6f9765ee-1ed2-47c9-9f38-da68d19b9e5f` (`spec_consistency`, `/root/merge_planning_product`)
- `92ce37e1-4647-48cb-a75b-53d0f1ba6b75` (`regression_risk`, `/root/architecture_regression_risk`)

Both are closed as replaced/orphaned bookkeeping only. This evidence does not claim either review passed, and neither stale result is used for current-HEAD readiness.
