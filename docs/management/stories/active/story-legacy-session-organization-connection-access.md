# Story: 旧セッションでも組織アプリを管理する

組織管理者として、ログインし直していない旧セッションでも、現在の組織に対応する外部アプリの接続状態を確認し、接続を開始したい。

## 受け入れ条件

- 旧JWTの `tenantId` が組織別名の場合、認証済みの組織情報から正規 `ten_*` を解決する。
- 正規tenantを解決できない場合は、組織接続APIが従来どおり拒否する。
- 正規 `ten_*` を持つ現行セッションは変更しない。

