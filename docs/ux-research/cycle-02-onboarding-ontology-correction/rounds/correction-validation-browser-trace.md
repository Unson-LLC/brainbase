# 訂正後の実ホスト操作記録

操作画面: リポジトリのビルド `bfaed02d72e643c6c5933b447371cc491d147089` にstdio接続したMCP Inspector 2.0.0。

評価者: 固定されたローテーション定義から32の合成ペルソナ視点を使ったCodex。ペルソナは評価視点であり、実在する参加者ではない。1つの共通実ホスト操作を評価したもので、32人が独立して操作したセッションではない。

## オンボーディング完了

1. `brainbase_onboarding_first_value` を開き、定義された7項目が実画面に表示されることを確認した。
2. Driveは接続済み、Gmailは認可待ちの状態で開始した。
3. 各成功結果に `runId`、`nextAction.tool`、説明、必要IDが含まれることを確認した。
4. 推論されたDecision候補を取り込んだ。
5. 直接承認を試し、エラーに安全な復旧方法として、人が確認した `edit` と `reject` の両方が示されることを確認した。
6. `edit` で候補を確定し、最初の価値を記録した。
7. 画面に前操作の `answerHash` と `usedCanonicalIds` が残った状態で `review=useful` を送った。
8. 最終結果が `first_value_answer_reviewed`、`nextAction: null` になることを確認した。

## オントロジー理解

1. 実ホスト画面で `get_ontology` を開いて実行した。
2. 結果が `beginnerGuide.oneSentence` から始まることを確認した。
3. ガイドに、型、関係、制約、推論、変更管理の5要素があることを確認した。
4. 変更不能なOntology 1.0.0契約より先に、次に使うツールが表示されることを確認した。

## 証拠の範囲

- 収集済み: 実ホスト画面、DOMスナップショット、スクリーンショット、MCPリクエスト履歴、自動テスト。
- 未収集: 実利用者の観察、実端末、スクリーンリーダー、ペルソナ別の所要時間。
