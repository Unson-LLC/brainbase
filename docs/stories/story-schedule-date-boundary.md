# Story: 会議APIの日付境界を保証する

## Goal

`/api/schedule/:date` が指定日以外の予定を返さないようにする。

## Acceptance Criteria

- timed eventは設定タイムゾーンで指定日に開始するものだけを返す。
- all-day eventは指定日を含む期間のものだけを返す。
- gogが終了境界のイベントを含めても翌日の予定を除外する。
- 既存の終日優先・時刻順ソートを維持する。

## KPI

- 期間: リリース後7日
- 指定日レスポンスへの翌日予定混入: 0件
