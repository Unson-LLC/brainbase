{
  "schema_version": "0.1.0",
  "story_id": "story-personal-value-proof-review-web-v1",
  "clauses": [
    {
      "id": "AC-001",
      "type": "security",
      "text": "The review host binds to 127.0.0.1 and serves only the shell, the bootstrap script, the two packaged UI files and the review API; the journal root resolves from --journal, then BRAINBASE_JUDGMENT_JOURNAL_DIR, then the default."
    },
    {
      "id": "AC-002",
      "type": "invariant",
      "text": "An unavailable journal or a malformed response is never rendered as zero items; the home shows three sections and the recording coverage including a possible stall."
    },
    {
      "id": "AC-003",
      "type": "behavior",
      "text": "The judgment card shows handling, decision, work impact, basis, prior learning reuse, execution, outcome and feedback in that order, and internal IDs appear only in the audit details."
    },
    {
      "id": "AC-004",
      "type": "security",
      "text": "Saving feedback requires the per-process token and same origin, rejects decisions absent from the journal, requires a reason for corrected and reverted, confirms the saved value by reloading, and keeps the draft on failure."
    },
    {
      "id": "AC-005",
      "type": "behavior",
      "text": "The consult action copies a request text that includes the decision attempt ID."
    },
    {
      "id": "AC-006",
      "type": "security",
      "text": "The review host rejects every request, including the shell, the review API and feedback, unless the Host header is 127.0.0.1, localhost or [::1] with the port the connection arrived on, so a rebound DNS name cannot read the token or judgments or write feedback."
    },
    {
      "id": "AC-007",
      "type": "behavior",
      "text": "The value-proof-review UI part lets a host that cannot read a local journal replace the unavailable notice title and guidance; the notice still shows the reason and never renders as zero items, and the default guidance stays the local --journal and environment variable hint."
    }
  ]
}
