---
name: context-check
description: テスト・ビルド・デプロイ前の実行環境確認手順。誤ディレクトリ実行やworktree混同を防ぐときに使用。
---

# Context Check Skill

**このSkillは、テスト・ビルド・デプロイ前に必ず実行する**

## 目的

コマンド実行時の重大なミス（間違ったディレクトリでのテスト実行など）を防ぐため、現在の実行環境を確認する。

## 実行内容

以下の情報を確認し、ユーザーに提示する：

1. **現在のディレクトリ** (`pwd`)
2. **Gitリポジトリ** (`git remote -v`)
3. **現在のブランチ** (`git branch --show-current`)
4. **Worktree構造** (`git worktree list`)

## 使用タイミング

### 必須（MUST）
- テスト実行前（`vitest`, `jest`, `npm test` など）
- ビルド実行前（`npm run build`, `next build` など）
- デプロイ実行前

### 推奨（SHOULD）
- 大規模なファイル操作前
- 複数プロジェクトを扱う作業の開始時

## 実行方法

### 1. Skillとして実行
```
/context-check
```

### 2. スクリプトとして実行
```bash
bash /Users/ksato/workspace/_ops/scripts/context-check.sh
```

### 3. npm scriptとして実行（salestailor）
```bash
npm run test:safe
```

## 出力例

```
════════════════════════════════════════════════════════════
📍 CONTEXT VALIDATION - 実行環境の確認
════════════════════════════════════════════════════════════

📂 Working Directory:
   /Users/ksato/workspace/shared/.worktrees/session-XXX-salestailor

🔗 Git Repository:
   https://github.com/Unson-LLC/salestailor-project.git

🌿 Current Branch:
   feature/salestailor-api

🌳 Worktree Structure:
/Users/ksato/workspace/projects/salestailor           main
/Users/ksato/workspace/.worktrees/session-XXX         feature/salestailor-api

════════════════════════════════════════════════════════════
```

## ルール

1. **テスト・ビルド前に必ず実行**すること
2. **現在のディレクトリで実行**すること（`cd`で移動しない）
3. **実行結果をTodoWriteに記録**すること

## 関連ドキュメント

- `/Users/ksato/workspace/CLAUDE.md` - 絶対ルール第8条
- `/Users/ksato/workspace/_ops/scripts/context-check.sh` - 実行スクリプト
