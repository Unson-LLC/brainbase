---
name: phase2-summary
description: Phase 1結果から画面サマリー生成+スケジュールファイル保存。
tools: Read, Write
---

# Phase 2: サマリー生成

## Purpose

Phase 1の収集結果を画面表示用Markdownにフォーマット化し、スケジュールファイルに保存。

## Input

- `/tmp/ohayo/phase1_results.json`

## Process

### Step 1: Phase 1結果読み込み

```bash
Read: /tmp/ohayo/phase1_results.json
```

### Step 2: 画面サマリー生成

以下のMarkdown形式で生成:

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
☀️ おはよう｜{曜日} {MM/DD}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📅 今日の予定: {count}件
📋 未完了タスク: {tasks.count}件
📊 昨日の活動: {git.count}件
💬 Slack未対応: {slack.count}件
📧 Gmail自動振り分け: {labeled}件完了（{total}件中）
📚 学習候補: {learning.queue_count}件

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Step 3: スケジュールファイル生成

ファイル名: `_schedules/YYYY-MM-DD.md`

### Step 4: ファイル保存

```bash
Write: /Users/ksato/workspace/shared/_schedules/{YYYY-MM-DD}.md
```

## Success Criteria

- [✅] SC-1: `phase1_results.json` 読み込み成功
- [✅] SC-2: 画面サマリー生成成功
- [✅] SC-3: スケジュールファイル保存成功（`_schedules/YYYY-MM-DD.md`）

## Output to Orchestrator

**成功時**:
```
✅ Phase 2 (Summary) 完了

画面サマリー生成完了
スケジュールファイル: _schedules/2026-01-03.md

Phase 3へ進行可能
```

**失敗時**:
```
❌ Phase 2 (Summary) 失敗

エラー: phase1_results.json が見つかりません
```

## Review & Replan Considerations

### Critical条件（Phase 2再実行）

- `phase1_results.json` 読み込み失敗
- スケジュールファイル保存失敗

### Minor条件（警告+進行）

- 一部データが空

### None条件（正常終了）

- 全ステップ成功
