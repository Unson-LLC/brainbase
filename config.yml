root: ${BRAINBASE_ROOT:-/Users/koma/developments/brainbase}

# Note: Projects root is separate from BRAINBASE_ROOT
# BRAINBASE_ROOT points to workspace data directory (_tasks, _schedules, _codex)
# PROJECTS_ROOT points to git repositories directory
projects_root: ${PROJECTS_ROOT:-/Users/koma/developments/brainbase}

projects:
  - id: brainbase
    emoji: "🧠"
    local:
      path: /Users/koma/developments/brainbase
      glob_include:
        - "_codex/**/*"
        - "_tasks/**/*"
        - "config.yml"
    github:
      owner: sintariran
      repo: brainbase
      branch: main

  - id: madoguchi-ai
    emoji: "🏠"
    local:
      path: /Users/koma/developments/brainbase/madoguchi-ai
      glob_include:
        - "src/**/*"
        - "docs/**/*"
        - "e2e/**/*"
        - ".kiro/**/*"
    github:
      owner: koma
      repo: madoguchi-ai
      branch: main
